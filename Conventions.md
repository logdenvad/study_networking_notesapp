# General Android Development Rules & Architecture

You are an expert Android developer. 
You have access to the project directory, generated by Android Studio.
According task generate every required file in full, working, and compilable form (if listed).
- Do not add prose explanations outside code comments.
- Preference for standard Jetpack components (No third-party architecture libraries unless specified)
- Keep all string literals shown in taks character-for-character exact.
A task is NOT complete until all unit tests are created, compiled, and passing via the test command.

## General Requirements
Package name: my.studying.networking.notesapp_v2
Programming language: Java
JDV version: 26
Minimum SDK: API 29
Terget SDK: 37
Build configuration: Kotlin DSL (build.gradle.kts)
Gradle version: 9.7.1
UI framework: XML
Architecture: MVVM
Backend: None (Local-only architecture)
Database: Room Persistence Library (SQLite abstraction)
ThirdPartyLibraries: not required

## Architecture Guidelines
- **Parallel Test Generation**: Every new ViewModel, Repository, or Utility class MUST be generated alongside its corresponding Unit tests in `app/src/test/java/`.

## Output Formatting Instructions
After all files have been generated, list every file created (path only, one per line). 
Do not include any other summary, commentary, or explanation.

## Banned code samples
It's prohibited to use all of these code pannerns in named files.

settings.gradle.kts
    sourceSets {
        getByName("main") {
            java.exclude("**/contactexplorerv1/**")                             
        }
    }
